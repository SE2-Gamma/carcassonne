pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}
rootProject.name = "Carcassonne"
include ':app'
include ':test'
project(':test').projectDir = new File(settingsDir, '../test')
include ':core'
project(':core').projectDir = new File(settingsDir, '../core')